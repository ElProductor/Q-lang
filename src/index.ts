// index.ts - Archivo principal de la extensión

console.log("¡Hola, mundo de TypeScript!");

// Función de ejemplo
function saludar(nombre: string): string {
  return `Hola, ${nombre}!`;
}

console.log(saludar("Juan"));
